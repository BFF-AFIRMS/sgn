<%init>
use CXGN::Phenotypes::Missing;

for my $format (@DOWNLOAD_MISSING_FORMATS ){
    my $selected = $format eq 'empty' ? 'selected' : '';
    if ($format eq 'empty'){
        print "<option value='$format' $selected>$format</option>";
    }
}
</%init>
